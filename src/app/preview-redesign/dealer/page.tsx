// Variant B preview route — local design review only
import { VariantBPreviewPage } from '../variant-b-preview';

export default function VariantBDealerPage() {
  return <VariantBPreviewPage page="dealer" />;
}
