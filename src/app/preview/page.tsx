// Variant B public preview alias — renders the same isolated preview design at /preview.
import { VariantBPreviewPage } from '../preview-redesign/variant-b-preview';

export default function VariantBPreviewAliasPage() {
  return <VariantBPreviewPage page="home" />;
}
